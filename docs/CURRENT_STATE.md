# Current state at repository HEAD

## Correction checkpoint at HEAD

- [x] Correct global finalist selection, complete compact model input, 41f46dd/5f1335b historical readback, optional capture isolation, and per-request fit rejection without retiring a healthy worker. Mixed cases no longer invoke the superseded probe-ID Laya route. Focused behavioral regressions cover these paths.
- [x] Measure cold startup separately from warm actual-model turns, with capture off for normal runs and opt-in worker-stage timing. The local speed targets and diagnostic utility are **not qualified**.
- [x] Bounded mixed-loop metadata continuation reaches and focuses relevant records at positions 130 and 181 without loading the full catalog. A 641-record sparse case that exceeds finite attention turns reports `NO_PROGRESS` and an explicit unscanned-catalog gap. Unavailable/degraded catalog attention and requested retrieval backlog likewise cannot masquerade as sufficient observability.
- [x] A drained mixed menu is rediscovered when later evidence changes the catalog generation; an unresolved old cursor still fails closed. Remaining eligible mixed work no longer waits for unrelated deep reasoning before the next owned turn.
- [x] A persisted contradictory pressure observation now changes the model-visible evidence generation and reaches a new comparative Laya turn while Qwen is active. The prior drained-session requeue defect is fixed. Actual Laya chose among four relevant stored-evidence retrievals, and the selected item was delivered through a custody-checked mixed-turn outcome. This proves scheduling and input flow, not a correct root-cause ranking or learned diagnostic redirect.
- [x] Bounded up-to-150 ms call-telemetry reuse reduces repeated warm worker admission cost while retaining worker identity, lease renewal, GPU identity, resource headroom, and freshness checks. The factory respects stricter 100–149 ms profile limits. Three matched capture-off actual-model trials, including the no-reuse baseline, are recorded in [Benchmarks and acceptance](BENCHMARKS_AND_ACCEPTANCE.md); both speed targets remain missed or unqualified.
- [x] The fixture-pilot exporter requires its independent outcome checker to bind the selected immutable snapshot/action, not merely accept a portable receipt. Fixture exports remain non-training-admissible and no Windows fault pilot has run.
- [ ] Run one consented reversible Windows fault with healthy/external controls and independent affected-task measurement. No such fault or pilot label is claimed at HEAD.

This page describes committed implementation only, not working-tree edits or host state. It is not a progress log; acceptance requirements live in [Benchmarks and acceptance](BENCHMARKS_AND_ACCEPTANCE.md).

## Available now

- A Python 3.12+ local-first investigator with a loopback case application and optional MCP adapter. The default install is deterministic; inference is opt-in.
- Seventeen registered read-only Windows collectors, fixed typed inputs, bounded isolated execution for default collectors, passive Event Log and connectivity capture, evidence coverage/provenance/redaction, SQLite persistence, retention, and hash-linked audit. Source observation, capture, case-open, and audit time have separate semantics. Custom trusted in-process handlers are not a hard-kill boundary.
- A bounded dependency/resource task scheduler, durable case state, hypotheses, attempt outcomes, cancellation/recovery, evidence and sourced reference graphs, and registered detail retrieval. Graph edges and keyword ranking do not prove causality.
- An event-driven mixed frontier can rank existing-evidence retrieval, observed graph branches, source-bound read-only measurements, and deep escalation while unrelated probes remain in flight. The scheduler owner validates a selected retrieval and permits another bounded fast turn within the same plan. Receipt-backed generic retrievals are excluded before discovery truncation, while explicit deep requests can revisit them. Selected deep work can overlap later fast turns. Candidate admissions, exact source bindings, and one-shot worker claims advance atomically; observation windows distinguish repeat measurements. A focus-delivery receipt records its exact source before the plan-end checkpoint, and recovery reports an unresolved gap rather than replaying an interrupted turn. The older synchronous/post-batch route remains for non-streaming cases and fallback.
- Structured semantic packets expose bounded nested GPU and process values, timestamps, quality, and omissions to the fast provider. Streaming receipts prioritize mandatory sources, admit a bounded first parent page, and mark later parent records deferred for exact retrieval. Source-validated measurement semantics expose bounded safe target/parameter hints and mask unknown selectors; an observed-identity graph bridge is navigation, not causal proof. Receipt-backed measurement snapshots can freeze all four mixed choice kinds for source revalidation.
- Replaceable fast and reasoning provider contracts, deterministic fallback, managed CUDA Laya, and local Ollama reasoning. An opt-in schema-v4 `warm-independent` profile and CLI path keep the two managed roles independently callable under one host lease budget; schema-v4 sequential swapping remains an explicit low-memory fallback. A controlled isolated-ledger run on this RTX 4090 produced real Laya rankings while the pinned local Ollama worker was active, and two non-degraded deep results were applied. This is a coexistence smoke test, not diagnostic qualification.
- An opt-in local worker draft retains the actual Laya collator input for an uncached selection of any mixed-frontier kind, tied to its immutable snapshot and worker presentation digest. Capture mode bypasses Laya's internal relevance cache; ordinary inference remains hash-only. Three isolated synthetic cases have captured exact worker inputs with installed-builder parity and a hidden-recipe, selected-action information-yield check. Their privacy review and training admission remain closed.
- Exact-scope consent and WinINet repair primitives with fake transport tests. They are not mounted as an application repair route and do not establish a real Windows repair.

## Not yet qualified or delivered

- The default installation is still deterministic. The warm profile is opt-in and development-only; any normal host run requires a compatible v4 lease ledger and verified resource headroom.
- Incremental invalidation and microbatch paths exist, but sustained useful judgment throughput and the all-attempt under-400-ms p95 target are not qualified. Three matched capture-off trials exposed 16 distinct offered item identities per case over 7 observed Laya menus at 4.145, 5.305, and 5.955 distinct items per active ranking second, below 20. Post-counterevidence delivery took 610, 574, and 494 ms from persistence, all above 400; these three samples do not establish a population p95. Each case accounted for 3 source triggers and 10 completed turns, while the changed event was eventually closed `no_new_fact` after the current bounded session had already used its evidence. No controlled real-fault diagnosis or repair was established. The synthetic drafts remain unreviewed and not training-ready.
- An isolated ordinary read-only Windows CLI run used deterministic providers, completed `core.system`, `core.resources`, and `pressure.sample`, and correctly stopped `insufficient_observability`; it is a host observation, not evidence of Laya/Qwen diagnostic accuracy.
- There is no measured held-out Windows diagnostic accuracy, training-admissible pilot corpus, trained Laya search policy, cloud inference, general automatic repair, or qualified real-fault repair run.
- The committed Qwen3.5 4B development pin was verified in this controlled coexistence smoke; it is not an optimal-model claim. Two test-only batch-eight Laya runs reduced worker microbatch counts but did not consistently improve case latency, so the development profile remains batch four. Qwen3.8 27B has not been qualified for safe simultaneous residency or context on this host.
