# Local development app

Run the app from this directory with:

```powershell
C:\Tools\SystemSenseRuntime\Scripts\python.exe app.py
```

Its health endpoint is `http://127.0.0.1:8000/health` and a healthy response is
HTTP 200 with body `ok`.
