[build-system]
requires = ["hatchling>=1.28,<2"]
build-backend = "hatchling.build"

[project]
name = "systemsense"
version = "0.1.0"
description = "Read-only Windows diagnostic evidence for AI agents"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "SystemSense contributors" }]
dependencies = [
    "mcp>=2.0,<3",
    "psutil>=7.0,<8",
    "pydantic>=2.12,<3",
    "pywin32>=311; sys_platform == 'win32'",
    "rich>=14.0,<15",
    "typer>=0.21,<1",
]

[project.scripts]
systemsense = "systemsense.cli:main"
systemsense-mcp = "systemsense.mcp_server:main"

[dependency-groups]
dev = [
    "build>=1.4,<2",
    "hypothesis>=6.151,<7",
    "pyright>=1.1.408,<2",
    "pytest>=9.0,<10",
    "pytest-cov>=7.0,<8",
    "ruff>=0.15,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/systemsense", "benchmarks"]

[tool.pyright]
include = ["src", "tests"]
exclude = [".venv"]
pythonVersion = "3.12"
typeCheckingMode = "strict"
venv = ".venv"
venvPath = "."

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "RUF", "UP"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]
